library calculator_lib;
import 'dart:math';

int add(int fNum, int sNum){
  return(fNum+sNum);
}
int mod(int fNum,int sNum){
  return(fNum%sNum);
}
int randomNum(int no){
return new Random().nextInt(no); 
}
